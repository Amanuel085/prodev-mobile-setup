# Task 2: Implementing Mobile Components in React Native

## ✅ Setup
- Initialized new Expo Router project: `npx create-expo-app@latest prodev-mobile-app-0x01 --template with-router`
- Removed default template files for a clean slate

## 🖥️ Component Implementation
- Created `app/index.tsx` with core components: `View`, `Text`
- Applied styles using `StyleSheet.create`
- Used nested `View` to group styled text blocks

## 🎨 Styling
- Background color: `#90caf9`
- Large text: red, small-caps, bold
- Medium text: purple, right-aligned
- Small text: blue, center-aligned

## 📱 Testing
- App launched successfully in Expo Go
- All styles rendered correctly on Android emulator